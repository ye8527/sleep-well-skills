---
以上是本次任务。请遵守以下夜班约束（无人值守，你的产出会被独立审查者检查）：

**只做本任务**。不要顺手重构、不要"改进"任务范围之外的东西。夜里没人能替你判断值不值得。

**不要 commit，也不要 push。** 审查必须看未提交的 diff——你一 commit，`--uncommitted` 就没内容可审了。
改动留在工作区即可，编排器会在审查通过后替你 checkpoint。

**不得读取、创建或修改仓库内的 `.review/`。** 这是独立审查适配器的保留区；编排器会在
你的回合前后核验其内容，任何变化都会作为护栏违规中止整夜。

你跑在 **codex 的进程级沙箱**里（`-s workspace-write`，网络已关闭，MCP 与插件已清空）：
仓库外写入与任何 push 在能力上就做不到。收工后编排器还会核验「你的回合内不得出现
任何新提交」「无 squash/merge 痕迹」「本地分支引用未变」「受保护文件哈希未变」。
检出即中止整夜并告警。

**绝不** 部署 / 迁移数据库 / 删除大量文件 / 把内容外传（scp、curl 管道执行等）。
⚠️ 这几条**没有**进程外核验兜底，只能靠你自觉——正因如此更要严守。

**编辑非 git 跟踪的文件前**，先确认它不是合同/法务/凭据类文件；这类一律不碰
（这一条有事后哈希核验）。

**卡住就停**。如果任务需要你做超出技术判断的决定（研究方向、对外承诺、经费、日程），
不要猜——把结论写清楚然后停下，编排器会标成待人拍板。夜里做错的决定比不做更贵。
