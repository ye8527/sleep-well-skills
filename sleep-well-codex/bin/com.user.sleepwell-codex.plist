<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.user.sleepwell-codex</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>-lc</string>
    <string>umask 077; mkdir -p "$HOME/Library/Logs/sleep-well" || exit 1; chmod 700 "$HOME/Library/Logs/sleep-well" || exit 1; exec "$HOME/.codex/skills/sleep-well-codex/bin/orchestrator.sh" >>"$HOME/Library/Logs/sleep-well/codex.out" 2>>"$HOME/Library/Logs/sleep-well/codex.err"</string>
  </array>
  <!-- launchd 会丢弃 job 运行期间的 interval 触发，因此不会启动并发实例；脚本内抢锁仅为手工调用与异常并发兜底。 -->
  <key>StartInterval</key>
  <integer>300</integer>
  <key>RunAtLoad</key>
  <false/>
  <!-- 注意：-lc 不保证 PATH 含 codex / claude / node；orchestrator.sh 会在既有 PATH 后追加常见安装位置。 -->
</dict>
</plist>
